segment-routing
 traffic-eng
  segment-list a
   index 1 ipv6-address 1000::179
  exit
  segment-list b
   index 2 ipv6-address 2000::179
  exit
  segment-list c
   index 10 ipv6-address 3000::179
  exit
  segment-list d
   index 10 ipv6-address 4000::179
  exit
  segment-list e
   index 10 ipv6-address 1001::179
  exit
  segment-list f
   index 10 ipv6-address 2001::179
  exit
  policy color 10 endpoint 1000::178
   candidate-path preference 1 name e explicit segment-list e weight 1
   candidate-path preference 1 name f explicit segment-list f weight 1
  exit
  policy color 1 endpoint 1000::178
   candidate-path preference 1 name a explicit segment-list a weight 1
   candidate-path preference 1 name c explicit segment-list c weight 1
  exit
  policy color 2 endpoint 2000::178
   candidate-path preference 2 name b explicit segment-list b weight 2
   candidate-path preference 2 name d explicit segment-list d weight 2
  exit
 exit
 srv6
exit